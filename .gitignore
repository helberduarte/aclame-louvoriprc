# Banco de dados local antigo (SQLite — não usado mais, mas por segurança)
voluts.db
voluts.db.*
*.bak

# Segredos e ambiente
.env
.env.local
.vercel

# Node
node_modules/

# Logs e temporários
*.log
